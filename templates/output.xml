<?xml version="1.0" encoding="UTF-8"?>
<pmh_approved_images>
    {% if images %}
        {% for image in images %}
            <image orig_filename="{{image}}">
                <new_filename>{{image.new_filename}}</new_filename>
                <terms>{{image.terms}}</terms>
            </image>
        {% endfor %}
    {% else %}
        <error>No approved images found</error>
    {% endif %}
</pmh_approved_images>
